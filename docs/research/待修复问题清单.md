# 稿件待修复问题清单（tex/ 全文审阅）

> 生成时间：2026-05-30
> 来源：对 `tex/` 全文的并行审阅（6 个审阅小组：前置 / 引言+方法论 / 预备知识+预训练 / 第5节 / 应用~结论 / 跨章节构建检查）。
> 说明：**论文当前可正常编译，无未定义引用/交叉引用**。下列均为内容与质量问题，不是编译错误。
> 行号为审阅时的快照，修改后可能略有偏移，请以实际内容为准。

## 已修复（不再列入待办）

- ✅ **H1** `table/graph_prompt_summary.tex:68` — 数学乱码 `\mathcal{G)_i}` → `\mathcal{G}_i`。（用户已修）
- ✅ **H3** `3.Preliminaries.tex:68-72` — 公式 (1) 类型不一致：已将 `\mathbf{g}(\cdot)` 明确定义为图变换、产出与 `\mathcal{G}` 同类型的图，并补充说明两个输入都是 `\varphi^*` 的合法图输入。

## 暂缓（用户明确暂不处理）

- ⏸ **H2** `2.methodology.tex:75-76` — "Survey Methodology" 一节缺少真正的方法论（检索数据库、检索式、纳入/排除标准、论文数量）。建议补一段简明的文献收集协议与语料规模。

---

## 🟠 中优先级（正确性 / 一致性）

### 引用与论断不符
| 位置 | 问题 | 建议 |
|------|------|------|
| `2.methodology.tex:100` | `yu2023selfsupervised` 被引为"图自监督学习"，实为**推荐系统**论文 | 删除或替换为图 SSL 综述 |
| `3.Preliminaries.tex:50` | Lester 等被归为 "prefix 类"，实为 prompt tuning | 改为"连续 prompt/prefix tuning"或拆分两类 |
| `4.Pre-training.tex:30` | "区分正负样本对"错误描述了 BGRL 等**无负样本**方法 | 改为"对齐增强视图（可含/不含显式负样本）" |
| `6.Applications.tex:7` | 社交网络论断（恶意账号识别、风险评估）所引文献不支持 | 补社交网络专门引用或弱化措辞 |

### 表格与正文矛盾（`table/graph_prompt_summary.tex`）
| 位置 | 问题 | 建议 |
|------|------|------|
| 第36行 vs `5.tex:29` | All-in-One 单元格写成 GPF 式**特征相加**，与正文"插入提示子图"矛盾 | 改写为图增强：提示节点、提示-提示边、提示-原图加权连边、整体编码 |
| 第55-63行 vs `5.tex:21,72` | PGCL 在表中标了 节点/边/图 三种下游任务，正文只当作图级方法 | 核对 PGCL 实际支持的下游任务并对齐 |
| 第78-82行 vs `5.tex:21` | SGL-PT 表中只标节点任务，正文称"图级表示学习" | 澄清"图级"指全局分支还是真正的图级下游任务 |
| 第9-16行等多行 | "预训练任务"勾选列含义模糊（自身目标 vs 兼容性） | 明确列含义并据此调整勾选 |

### 结构 / 位置
| 位置 | 问题 | 建议 |
|------|------|------|
| `4.Pre-training.tex:44` / `5.tex:2` | 大汇总表从第4节 `\input`，却在第5节才首次引用 | 将表移到第5节引用处附近 |
| `1.intro.tex:77-91` vs `2.methodology.tex:62-68` vs `5.tex:2` | 研究问题编号不一致：引言 RQ1–RQ5、方法论表 Q1–Q5（措辞不同）、第5节只提4个问题 | 统一编号与措辞 |
| `1.intro.tex:121` vs `9.Conclusion.tex` | 路线图承诺结论含"作者贡献声明"，但结论中没有 | 删除该承诺或补上声明 |
| `8.Discussion.tex:23-39` vs 8-13行 | 未来方向遗漏"部署/评估"，而前文列为挑战 | 补一条领域评估/部署方向 |
| `8.Discussion.tex:37-38` | 可疑被注释引用 `% , zhang2021protgnn}`，自解释性论断证据不足 | 恢复引用或补图可解释性引用 |
| `5.tex` (1行与108行) | 单个文件含两个顶层 `\section` | 考虑拆成独立文件 |

### 图片问题
| 位置 | 问题 | 建议 |
|------|------|------|
| `1.intro.tex:60`（图 `pt.pdf`） | 图中可见拼写错误 "Prompt **turning**" | 应为 "Prompt tuning"，需重新生成图 |

### ProG 一节
| 位置 | 问题 | 建议 |
|------|------|------|
| `7.ProG.tex:2` | 宣传性措辞（indispensable、plethora、promises to significantly enhance） | 改为中性综述语气并补证据 |
| `7.ProG.tex:7-12` | 数据集列表含被注释引用、措辞"Pubmed etc." | 恢复数据集引用或删注释；写"PubMed, and others" |
| `7.ProG.tex:13` | "state-of-the-art" 对 2022–2023 方法可能过期/夸大 | 改为"representative methods" |

---

## 🟡 低优先级（写作 / 清理）

### 反复出现：被注释掉的引用削弱论断
> 要么恢复、要么彻底删除，避免论断悬空。
- `1.intro.tex:4-11, 25-31` — CV/多模态（CLIP/BLIP 类）引用被注释。
- `3.Preliminaries.tex:15-19` — 矩阵分解基础引用（Laplacian Eigenmaps、SDNE、GraRep）被注释。
- `3.Preliminaries.tex:38-40` — GAE/VGAE 基础引用 `kipf2016variational` 被注释。
- `7.ProG.tex:7-12` — 数据集引用被注释。
- `8.Discussion.tex:37` — `zhang2021protgnn` 被注释。

### 术语一致性（全局）
- "graph prompt learning" 出现 6 种大小写写法；"graph prompt / graph prompting / graph prompts" 混用。
- 建议规范：正文用句首大写；领域统一用 "graph prompting"；具体产物用 "graph prompts"；标题/小节标题用 title case。

### 前置部分（`0.main.tex` / `Ack.tex` / `author.tex`）
| 位置 | 问题 | 建议 |
|------|------|------|
| `0.main.tex:61-90,113,128,134-135` | 作者块、`\input{author}`、`\input{Ack}` 全被注释（当前不显示作者与致谢） | 投稿前激活最终结构 |
| `0.main.tex:11,27` | `hyperref` 在 `natbib` 之前加载 | 调整顺序，`hyperref` 尽量靠后 |
| `0.main.tex:14,131` | `ulem` 未加 `normalem`，全文 `\emph` 变下划线 | 改为 `\usepackage[normalem]{ulem}` |
| `Ack.tex:3` | "Research Grant Council" → "Research Grants Council"；含 `\dotuline` 草稿标记句；NSFC 资助语法 | 改写并去草稿标记 |
| `author.tex:3` | LaTeX 引号写法 `''...''` 错误 | 用 ``...'' |
| `author.tex` 多处 | 作者简介语法问题（"master student"、"received Ph.D. degree" 等） | 通读润色 |

### 语法 / 拼写
- `7.ProG.tex:20` "Easy Extendable" → "Easily Extensible"。
- `3.Preliminaries.tex:38` 主谓不一致（"Pre-training and fine-tuning has..."）。
- `5.tex:85` "project vector" → "projection vector"；HetGPT 与 GraphPrompt 衔接含混。
- `1.intro.tex:52-63` 图注语法与过度类比（"in the same way" 与70行表述矛盾）。

### 标签 / 引用卫生
- `2.methodology.tex:76,85-87` — 标签 `fig:top10keys` 与标题 "Top 15 Keywords" 不符；图注 "Keywords appeared" 语法。建议改标签为 `fig:top15keys`。
- 若干已定义但未被引用的标签（`fig:intro_lang_prompt`、`fig:intro_graph_prompt`、`fig:PromptExample`、`fig:credit_case`、`fig:pre1` 等）：补引用或删除。
- 编译日志 **319 条字体替换警告**（Palatino/`TU/ppl`）：仅外观，优先级低。

### 注释残留 / 未完成内容
- `1.intro.tex:45-50` 重复图块（复用 `fig:PromptExample` 标签）。
- `2.methodology.tex:9-19` 注释块自带"the following content can be removed"。
- `3.Preliminaries.tex:59,65` 遗留旧散文与 typo "tunning"。

---

## ⚪ `tex/archive/`（不参与编译，可整体处理）
跨章节检查报出的下列"错误"全部位于 `tex/archive/`，**不在构建中**，当前无害：
- 重复标签：`sec:intro`、`sec:method`、`subsec:pdomain`。
- 缺失引用键：`anonymous2023one`（仅在 archive 中被引）。
- 缺失图片：`pic/Taxonomy`（仅 `tex/pic/taxonomy.tex` 存在）。
- 大量红色占位标记 `\red{...}`、草稿时间线等。

**建议**：将 `tex/archive/` 移出 TeX 目录树（或加入 `.gitignore`/重命名），避免日后审阅与构建混淆。

---

## 建议的修复优先顺序
1. ~~H1 乱码公式~~（已修）、~~H3 公式类型~~（已修）
2. All-in-One 表格与正文矛盾（中）
3. RQ/Q 编号不一致（中）
4. `pt.pdf` "Prompt turning" 拼写（中）
5. 引用与论断不符的 4 处（中）
6. 前置部分激活作者/致谢、`ulem normalem`（低但投稿前必做）
7. 术语统一 + 注释引用清理（低，批量处理）
