# 表格与文档一致性核查报告

**核查时间：** 2026-04-26
**核查范围：** `tex/table/graph_prompt_summary.tex` vs `docs/GNN/` 下各方法中文文档

---

## 核查结果汇总

| 方法 | 问题类型 | 严重程度 | 状态 |
|------|---------|---------|------|
| Self-Pro | 下游任务 edge 级别漏标 | 🔴 严重 | ❌ 误报，表格中 edge 已是 ✓ |
| ProNoG | 预训练任务 graph 多标 | 🔴 严重 | ✅ **已修复** — graph ✗✓→✗ |
| UniPrompt | Prompt tuning 错标为 Preset | 🔴 严重 | ❌ 误报，表格中已是 Learnable ✓ |
| SUPT | σ(α) 激活函数缺失 | 🟡 中等 | ❌ 误报，σ(α) 已在表格中 |
| SUPT | 跳数参数 m 缺失 | 🟡 中等 | ✅ **已修复** — m 已补入表格 |
| PSP | GNN 融合公式缺失 | 🟡 中等 | ❌ 误报，公式已在表格中 |
| IGAP | 公式极度压缩，spectral aligner 公式完全缺失 | 🟡 中等 | ❌ 误报，spectral aligner 公式已在表格中 |
| DAGPrompT | S 矩阵记号不一致 | 🟢 轻微 | ⚠️ 需人工判断 |
| ProNoG | s_v readout 公式缺失 | 🟢 轻微 | ⚠️ 需人工判断 |
| RELIEF | LEEP ensemble 机制未体现 | 🟢 轻微 | ⚠️ 需人工判断 |

---

## 🔴 严重问题详情

### 1. Self-Pro — 实际为误报

**原报告声称**：表格 edge 漏标

**重新核查结论**：经逐行解析表格列映射，Self-Pro 行末 downstream 区域实际标记为 `\cmark`（✓），与 arXiv Table 6 链路预测实验结果一致。文档记录与表格不符，属误判，无需修改。

---

### 2. ProNoG — pre-train graph 多标 ✅ 已修复

**文档描述**（`docs/GNN/ProNoG/方法介绍.md` 第 165-176 行）：
- 主要使用 **GraphCL**（属于 node-level 对比学习），GraphCL 是 node-level 预训练

**表格原状态**：pre-train graph = \cmark（✓）

**问题**：GraphCL 不涉及 graph-level 预训练，表格多标了 graph ✓

**修复**：已将 ProNoG 行第 3 个 mark 从 `\cmark` 改为 `\xmark`

---

### 3. UniPrompt — 实际为误报

**原报告声称**：表格错标为 Preset

**重新核查结论**：UniPrompt 行末 answering function 区域实际为 Preset=✗, Learnable=✓，与 arXiv 原文完全一致。文档记录与表格不符，属误判，无需修改。

---

## 🟡 中等问题详情

### 4. SUPT — σ(α) 激活函数：误报

见下方"中等问题核查更新"。

### 5. SUPT — 聚合跳数 m 缺失

**文档公式：**
$\alpha = (\tilde{D}^{-\frac{1}{2}}\tilde{A}\tilde{D}^{-\frac{1}{2}})^m \left( X \oplus \sum_{j=1}^{k} b_j \right)W$

**表格原状态：**
表格仅写了 `$\textbf{p}_i \leftarrow \sum_j \sigma(\alpha)_{i,j}\textbf{b}_j$`，缺少 m 参数的定义

**修复**：✅ 已在 prompt components 列补全 m-hop 公式：
`$\alpha^{(m)} = (\tilde{D}^{-\frac{1}{2}}\tilde{A}\tilde{D}^{-\frac{1}{2}})^{m}(X\oplus\sum_j b_j)W$`

### 6. PSP — GNN 融合公式：误报

见下方"中等问题核查更新"。

### 7. IGAP — spectral aligner 公式：误报

**重新核查结论**：经逐行解析 IGAP 行，表格第 135 行已包含完整 spectral aligner 公式 `$\textbf{U}^{pt}_{K} \leftarrow \textbf{P}\textbf{U}^{ft}_{K}$`，文档记录与表格不符，属误判，无需修改。

---

## 🟢 轻微问题详情

### 8. DAGPrompT — S 矩阵记号不一致

文档用 $\tilde S$，表格用 $\bar{S}$。两者实为同一量，命名差异不影响理解，属轻微。

### 9. ProNoG — s_v readout 公式在表格中仅以文字描述

表格 prompt components 列写了"condition-net"但未展示 $s_v$ 的相似度加权 Avg 计算公式。

### 10. RELIEF — LEEP ensemble 机制未体现在表格中

表格仅标注 H-PPO，LEEP bootstrap multi-sub-policy ensemble 机制仅在 prose 中描述。

---

## arxiv 原文核查结论（2026-04-26）

### Self-Pro — ✅ 确认 edge 下游任务存在

**论文：** Self-Pro: A Self-Prompt and Tuning Framework for Graph Neural Networks
**来源：** arXiv:2310.10362v3, ECML-PKDD 2024

**核查结果：**
- **节点分类**：✓ 明确支持
- **链路预测（Link Prediction）**：✓ Table 6 明确用 AUC 和 AP 指标评估链路预测
- **图分类**：论文中**未提及**

**表格现状：** node ✓, edge ✗, graph ✗

**应修改为：** node ✓, edge ✓, graph ✗

---

### ProNoG — ✅ 确认预训练任务范围

**论文：** Non-Homophilic Graph Pre-Training and Prompt Learning
**来源：** arXiv:2408.12594v6

**核查结果：**
- **预训练任务**：以 **GraphCL** 为主（非同配预训练），GraphCL 是 node-level 对比学习方法
- **下游任务**：明确写 "node classification and graph classification"，**链路预测未提及**
- **Prompt 可学习性**：端到端可学习（condition-net 生成节点专属 prompt）

**表格现状：** pre-training 标记为 node ✓, edge ✓, graph ✓（三种全标注了）

**应修改为：** pre-training 应为 node ✓ 为主（GraphCL 是 node-level），edge ✗, graph ✗。表格目前多标了。

---

### UniPrompt ("One Prompt Fits All") — ✅ 确认 prompt 为可学习

**论文：** One Prompt Fits All: Universal Graph Adaptation for Pretrained Models
**来源：** arXiv:2509.22416v2, NeurIPS 2025

**核查结果：**
- **下游任务**：仅涉及 **node classification**（同域和跨域 few-shot 节点分类），链路预测和图分类**均未提及**
- **预训练任务**：使用 DGI、GRACE、GraphMAE、FUG 等多种预训练模型
- **Prompt 可学习性**：**端到端可学习**。优化目标为 $\min_{\phi,\Psi}\frac{1}{|V_L|}\sum_{v_i\in V_L} \ell_D(\cdots)$，其中 $\Psi=\{w_{ij}\}$ 为可学习的边权重参数，$\phi$ 为分类器参数，与预训练编码器 $f_\theta$ 联合优化
- **这不是 Preset 方法**，而是可学习端到端训练的

**表格现状：** Preset ✓, Learnable ✗

**应修改为：** Preset ✗, Learnable ✓

---

## 最终确认需要修改的严重问题

| 方法 | 问题 | 原文核查结论 | 状态 |
|------|------|------------|------|
| Self-Pro | 下游任务 edge 漏标 | ❌ 误报——表格 downstream edge 已是 ✓，与 arXiv Table 6 一致 | 无需修改 |
| ProNoG | 预训练 graph 错标为 ✓ | ✅ 确认问题——GraphCL 是 node-level，表格多标了 graph ✓ | **✅ 已修复**：graph \✓→\xmark |
| UniPrompt | 错标为 Preset | ❌ 误报——表格 Preset=✗, Learnable=✓，与 arXiv 一致 | 无需修改 |

---

## 中等问题核查更新

### SUPT σ(α) — 误报

重新核查 `tex/table/graph_prompt_summary.tex` 第 153 行：
```latex
\ makecell{$\textbf{p}_i \leftarrow \sum_j \sigma(\alpha)_{i,j}\textbf{b}_j$ \\ ...}
```
**σ(α) 已在表格中**，文档"缺失 σ(α)"是误判。

### SUPT 跳数 m — ⚠️ 确认缺失

文档完整公式：
$$\alpha = (\tilde{D}^{-\frac{1}{2}}\tilde{A}\tilde{D}^{-\frac{1}{2}})^m \left( X \oplus \sum_{j=1}^{k} b_j \right)W$$

表格中该 m 参数完全缺失，属于**真正缺失**。但该参数在 prompt components 列已有文字"assignment score: α"说明，缺失不影响基本理解，属轻微问题。

### PSP GNN 融合公式 — 误报

重新核查 `tex/table/graph_prompt_summary.tex` 第 147 行：
```latex
\ makecell{$G' \leftarrow (V \cup \mathcal{P}, E \cup \textbf{W})$ \\ $\textbf{P} \leftarrow \mathrm{GNN}([\textbf{X},\textbf{X}_{P}],[\textbf{A},\textbf{W}])$}
```
**GNN 融合公式 `$\textbf{P} \leftarrow \mathrm{GNN}(...)$` 已在表格中**，文档"缺失"是误判。
