# `graph_prompt_summary.tex` 表格审查问题清单

> 生成时间：2026-06-03
> 对象：`tex/table/graph_prompt_summary.tex`（22 个方法、12 列的代表性图提示方法对比表）
> 来源：4 个并行审阅小组（LaTeX 结构 / 内容前11行 / 内容后11行 / 表↔正文一致性）。
> 结论先行：**表格结构正确、可正常编译**（所有行 12 单元格、括号配对、无溢出）。问题集中在**内容准确性**与**表↔正文一致性**。
> 列含义：Paper | 预训练任务{node,edge,graph} | prompt design{prompt components, inserting pattern, prompt tuning} | 下游任务{node,edge,graph} | answering function{Preset, Learnable}。
> 行号为审阅快照，修改后可能偏移，以实际内容为准。

---

## ✅ 已修复

- **H1（重复条目）** — SAP 行 与 PSP 行是同一篇论文。
  - 经 web 核实：arXiv:2310.17394《Enhancing GNN with Structure-Based Prompt》= PSP（ECML PKDD 2024）预印本，正式名 **PSP**，`ge2023enhancing` 与 `ge2024psp` 为重复 bib。
  - 处理：统一为 PSP（`ge2024psp`），归类为图结构型方法，删除全部 "SAP"/`ge2023enhancing`。改动 `table/graph_prompt_summary.tex`、`5.tex`、`1.intro.tex`、`8.Discussion.tex`、`pic/taxonomy.tex`、`zotero.bib`。
  - 验证：重编译无未定义引用，新 `.aux` 中 `ge2023enhancing` 为 0，活跃源文件无 SAP 残留（仅 `archive/` 不参与编译）。

---

## 🔴 High — 待处理

| # | 位置 | 问题 | 建议修改 |
|---|------|------|----------|
| H2 | All in One (33–40) | **插入机制写错**。第36行用 GPF 式特征相加 `x_i + Σ w_ik p_k`；All in One 实际是**插入提示子图**（提示节点 + 内部连边 + 与原图加权交叉连边），再整体编码。与正文 `5.tex:31` 矛盾。 | 改为图拼接记法，如 `G'=(V∪P, E∪E_P∪E_cross)` 配交叉连边权重。 |
| H3 | PGCL (55–63) | **下游任务勾选夸大**。表标 node+edge+graph，正文(`5.tex:23,74`)定位为图级方法；tuning 损失用 `(v,a,b)` 三元组，读起来像节点/边级，与图级多视图对比不符。 | 下游改为仅 graph；损失改为图/视图级 InfoNCE 记法。 |
| H4 | DeepGPT (105–112) | **预训练勾选无依据**。标 node+edge+graph 三种 pretext，但 DeepGPT 是面向 graph Transformer 的 prompt tuning，并不假设三种 pretext。 | 仅标实际支持项，或写 "depends on backbone"。 |
| H5 | 覆盖缺失 | 正文 `5.tex` 反复讨论 **HetGPT**(行25,54,74 等) 与 **GraphControl**，但表中**完全没有**。 | 补行，或在 caption 注明表为"代表性子集"。 |

---

## 🟠 Medium — 待处理

### 公式未忠实反映方法
| 位置 | 问题 | 建议 |
|------|------|------|
| GPF-Plus (89) | 写成 `x_i + σ(p_1,…,p_k)`，像单一全局 prompt | 改为按节点加权组合 `p_i=Σ α_ik p_k, x_i*=x_i+p_i` |
| IGAP (136) | tuning 仅写 `InfoNCE_cls`，漏掉核心谱信号/结构对齐损失 | 补 signal-prompt + spectral-aligner 损失 |
| UniPrompt (164–166) | 邻接矩阵构造公式与"保持输入图"目标存在张力 | 核对原文公式后修正 |

### 属性 / venue / 名称
| 位置 | 问题 | 建议 |
|------|------|------|
| GPF (25) | 标 "(arXiv)" 引 `fang2022prompt`；GPF/GPF-Plus 常作为 `fang2023universal`(NeurIPS 2023) 两变体 | 明确该行代表早期 GPF 还是 Universal Prompt Tuning |
| SUPT (151) | venue 过期，arXiv 现已有期刊 "Information Sciences 749 (2026)" | 更新 bib 与单元格为期刊版 |

### 勾选可疑（建议逐一对照原论文核实预训练/下游级别）
- **DAGPrompT (114)**：仅标 edge 预训练，论文涉及更广 SSL（link prediction/DGI）。
- **IGAP (133)**：标 edge+graph 不标 node，存疑。
- **VNT (187)**：标 node+edge 预训练，与 KDD few-shot 节点分类设定不符。
- **ULTRA-DP (121)**：正文(`5.tex:74`)说"边预测+邻域预测"，表中只标 edge。
- **SAP→PSP (已并入 PSP 行)**：原 SAP 标 node+graph 下游，正文只描述节点级对比；并入后以 PSP 行(145–149)为准，需复核 PSP 下游范围。

### 表↔正文其它矛盾
- **GraphPrompt (45–52)**：表标 Preset only，正文(行54附近)说常需额外任务模块。
- **SGL-PT (78–82)**：表只标 node 下游，正文(行23)说"图级表示学习"——需澄清是表示学习还是图级下游任务。

### 术语 / 列设计
- 列名 "Preset" vs 正文 "Hand-crafted Answering Function"；"prompt tuning" 归在 "prompt design" 下，正文却当作独立的学习问题——分组/命名不一致。
- 表行序既非时间序也非正文 taxonomy 序（GPF 与 GPF-Plus 被隔开，PSP/VNT 排末尾），建议按 taxonomy 分组或按年份排序。

---

## 🟡 Low — 待处理

- **PRODIGY (70)** "Fixed"、**DAGPrompT (117)** "Similarity Loss" 作为 tuning 目标太含糊，建议具体化。
- **caption 图例 (3)**：`\tilde{s}` 标注 "Filled prompt" 含糊，且多行实际用 `\tilde{x}`/`X*`/`\tilde{h}`；`τ, δ, σ, ⊙, ⊕` 未在图例解释。建议把 `\tilde{s}` 改为"prompted/filled representation"并补符号说明。
- **可读性 (6)**：`\resizebox{0.99\textwidth}` 套 12 列公式表，在 IEEE 双栏 `table*` 下渲染偏小。建议拆表 / 用 `tabularx` / 公式移附录。
- **Q2/Q4 可追溯性**：`5.tex` 的 Question 2（任务重构）与 Question 4（联系/优劣）在表中无对应列，可加列或调整声明。

---

## 建议处理顺序
1. ~~H1 删除 SAP 重复行~~（✅ 已完成）
2. H2 / H3：All in One 插入机制、PGCL 下游范围（表与正文同步改）
3. H5：补 HetGPT/GraphControl，或 caption 注明"代表性子集"
4. H4 + Medium 勾选问题：逐一核对原论文，并先在 caption 明确"预训练任务"列语义（"方法自身使用的预训练目标" vs "兼容的预训练任务"）
5. Low 项批量收尾
