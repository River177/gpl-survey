\section{Prompt Design for Graph Tasks}\label{sec:ptask}
After reviewing graph pre-training objectives, we now turn to how prompts are designed and used to adapt pre-trained graph models. As shown in \figref{fig:prompt}, a graph prompt usually contains three components: prompt tokens with prompt vectors, token structures that preserve correlations among these tokens, and inserting patterns that specify how prompts are integrated with the original graph. Based on these components, and following \textbf{RQ3} (Section~\ref{sec:method}), we analyze prompt design along four aspects: how a graph prompt is designed; how downstream tasks are reformulated toward the pretext; how a graph prompt is learned; and what the connections, pros, and cons of existing designs are. With these aspects, we summarize representative methods in \tabref{tab:graph_prompt_summary}.

\input{table/graph_prompt_summary}


\begin{figure}[t]
    \centering
    \includegraphics[width=0.49\textwidth]{pic/prompt.pdf}
    \caption{Prompt Tokens, Structures, and Inserting Patterns.}
    \label{fig:prompt}
\end{figure}



\subsection{Prompt Token, Structure, and Inserting Pattern}\label{subsec:pdesign}

\textit{\dotuline{How do they design a graph prompt?}}

\textbf{A. Prompt as Tokens. }
The simplest graph prompts can be represented as additional feature vectors attached to the original graph features \cite{zhu2023graphcontrol}. Given a graph feature matrix $\mathbf{X}=\{x_1,\cdots, x_N\} \in \mathbb{R}^{N\times d}$, where $x_i\in \mathbb{R}^{1\times d}$ denotes the feature of node $v_i$, \citet{fang2022prompt} and \citet{shirkavand2023deep} define a learnable prompt vector $p \in \mathbb{R}^{1\times d}$ and add it to each node feature, producing $\mathbf{X}^{*}=\{x_1+p,\cdots, x_N+p\}$. The prompted feature matrix is then fed into a pre-trained graph model. GPF-Plus \cite{fang2023universal} extends this idea from a single shared token to multiple basis tokens, which increases the capacity of feature-level prompting while keeping the prompt lightweight.

Other methods use token prompts in more specialized ways. PGCL \cite{gong2023prompt} designs separate prompt vectors for semantic and contextual views and applies them to graph-level representations through element-wise multiplication. VNT \cite{tan2023virtual} instead concatenates prompt tokens with the original node set and lets a graph transformer integrate them through self-attention, so the prompt interaction is learned inside the transformer rather than through explicit prompt-node edges. GraphPrompt \cite{liu2023graphprompt} places the prompt in the hidden representation space and uses it to guide the readout operation. Given node embeddings $\{\textbf{h}_v:v\in\mathcal{V}\}$ and a task-specific prompt token $\textbf{p}_t$, it computes $\textbf{s}_t=\text{Readout}(\{\textbf{p}_t \otimes \textbf{h}_v:v \in \mathcal{V}\})$. SGL-PT \cite{zhu2023sglpt} similarly introduces a prompt token connected to all graph nodes, so that the prompt can provide global information for graph-level representation learning. GPPT \cite{sun2022gppt} defines task tokens and structure tokens to align node classification with link prediction: the label is represented as a task token, the target node neighborhood is represented as a structure token, and prediction is reformulated as estimating the link between the two.

Later token-based designs further relax the assumption that prompts must be a single vector shared by all nodes. ULTRA-DP \cite{chen2023ultradp} creates a target-node-specific prompt by combining positional embeddings with task embeddings from pre-training. HetGPT \cite{ma2023hetgpt} uses node tokens and class tokens, and adds type-specific feature tokens to handle heterogeneous graphs. Self-Pro \cite{gong2024selfpro} constructs semantic and structural prompts from the input graph and reuses the pre-trained projector as a downstream adapter. EdgePrompt \cite{fu2025edge} shifts part of the prompt from nodes to edges and injects edge prompts during message passing. ProNoG \cite{yu2025pronog} targets non-homophilic graphs by generating node-specific prompts from similar neighbors in the local subgraph.




\textbf{B. Prompt as Graphs. }
Some methods represent the prompt as an explicit graph rather than as isolated feature vectors. In All in One \cite{sun2023all}, the prompt is a learnable subgraph whose nodes have the same representation dimension as the original graph nodes. Its token structure consists of links among prompt nodes and cross-links between prompt nodes and original nodes, with link weights computed from feature similarity. The prompted graph is formed by attaching this prompt subgraph to the input graph and then passing the combined graph to the pre-trained encoder.

PRODIGY \cite{huang2023prodigy} uses a different graph-form prompt to support graph in-context learning. Its prompt construction contains data graphs and a task graph. Data graphs correspond to task instances, such as subgraphs around target nodes, node pairs, or graph classification examples. The task graph connects data tokens and label tokens, and pre-training is formulated through objectives such as neighboring matching and label matching. This design uses the prompt graph to provide a unified interface for upstream and downstream tasks. PSP \cite{ge2024psp} also attaches class prototype nodes to the original graph through learnable weighted links, and aligns the prompted graph with a node-level contrastive objective by comparing MLP-encoded node features with GNN-encoded prompted representations.

Later graph-form prompts focus more on selecting, aligning, or transforming the prompt structure itself. GraphPrompter \cite{lv2025graphprompter} maintains a cache of candidate prompt subgraphs and selects relevant ones through a learned task-graph selector. IGAP \cite{yan2024igap} instead works in the spectral domain, using signal prompts and a spectral aligner to bridge the pre-training and fine-tuning graph subspaces.


\subsection{Aligning Tasks by Answering Function}\label{subsec:align}

\textit{\dotuline{How do they reformulate downstream tasks to the pretext?} }

Once the prompt has been inserted, the next issue is how the prompted graph produces an answer. Most methods either reshape downstream instances to match a pre-training objective or add an answering function on top of the prompted representation.

\textbf{A. Handling Different Level Tasks.} 
All in One \cite{sun2023all} pre-trains the graph model with graph-level contrastive learning, where the encoder learns from different views generated by perturbing the original graph. To connect node-, edge-, and graph-level downstream tasks with this graph-level pretext, it converts node and edge instances into induced subgraphs, a strategy also used in related task-reformulation methods \cite{huang2023prodigy,liu2023graphprompt,gong2023prompt}. From this view, a graph prompt can be interpreted as a learnable graph modification that approximates operations such as feature masking, edge perturbation, or subgraph transformation. The practical goal is not to claim that a prompt can simulate every graph operation, but to make the downstream instance closer to the representation interface learned during pre-training.


\textbf{B. Learnable Answering Function.} 
To produce downstream predictions, \citet{sun2023all} consider two types of answering functions. The first type is a learnable task head, such as an MLP, that maps the prompted graph representation to the downstream label space. For example, in a three-class node classification task, the target node can first be converted into an induced graph, combined with a graph prompt, encoded by the pre-trained model, and then classified by a three-output prediction head. In this setting, both the prompt and the task head may be tuned. Similar learnable answering functions are also adopted in \cite{fang2022prompt, fang2023universal,tan2023virtual}. These task heads make prediction flexible, but they also introduce an additional tuned component beyond the prompt itself.



\textbf{C. Hand-crafted Answering Function.} 
The second type uses a preset answering rule without adding a trainable task head. In All in One \cite{sun2023all}, a node classification task can be handled by constructing $K$ label-specific sub-prompts, where $K$ is the number of classes, and assigning the target node to the class whose prompted graph is most similar to the target instance under a graph-level contrastive objective such as GraphCL \cite{you2020graph}. GPPT \cite{sun2022gppt} and HetGPT \cite{ma2023hetgpt} use link prediction as the task template: the node label is represented as an additional token, and classification is reformulated as estimating the likelihood of a link between the label token and the target node. 
PSP \cite{ge2024psp} compares node representations from two graph views, one produced by node feature encoding and the other by a GNN over the prompted graph. Its class prompt tokens are used as reference representations, and prediction is based on similarity to these class tokens. PRODIGY \cite{huang2023prodigy} uses a task graph to describe node-, edge-, and graph-level classification through links between data tokens and label tokens. GraphPrompt \cite{liu2023graphprompt} extends link prediction to graph-pair similarity and aligns node and graph classification with this pre-training interface. Given induced graphs $(g_1,g_2,g_3)$ where $g_1$ and $g_2$ share the same label and $g_1$ and $g_3$ have different labels, the answering template encourages $\text{sim}(g_1,g_2)>\text{sim}(g_1,g_3)$. For node classification, the induced graph is the contextual subgraph around the target node.



\subsection{Prompt Tuning}\label{subsec:pt}

\textit{\dotuline{How do they learn a graph prompt? }}

The answering function determines how predictions are read out, but the prompt itself still has to be learned. Existing methods differ mainly in whether they learn prompts across tasks, for a specific downstream task, or under an objective aligned with the pretext task.

\textbf{A. Meta-Learning Technique. }
To learn appropriate prompts, \citet{sun2023all}  leverage meta-learning techniques (such as MAML\cite{finn2017modelagnostic} model) to obtain a robust starting point for the prompt parameters. Since the support set and query set include various graph tasks (such as node classification, link prediction, graph classification, etc), the learned graph prompt is expected to be more general on various downstream tasks.

\textbf{B. Task-specific Tuning. }
Besides All in One \cite{sun2023all}, which aims to learn a general prompt on various downstream tasks, there are also some works that target specific downstream tasks such as graph classification. In this case, the prompt tuning can be more task-directed. For example, GPF \cite{fang2022prompt} aims at a graph classification task, so it just needs to tune the prompt token $\textbf{p}$ and the task head $\phi$ by maximizing the likelihood of predicted correct graph labels given the prompted graph representation $\tilde{\textbf{s}}_i$ from the pre-trained graph model $\pi$. In this case, the task head tuning and the prompt tuning share the same objectives, which can be formulated by: $\max_{\textbf{p}, \phi} \sum_{(y_i,\tilde{\textbf{s}}_i)} p_{\pi, \phi} (y_i| \tilde{\textbf{s}}_i)$. SUPT \cite{lee2024supt} extends GPF-Plus to the subgraph level by aggregating information from the $m$-hop neighborhood when computing prompt assignments, so that prompts can reflect the local graph structure around each node.



\textbf{C. Tuning in Line with Pretext. }
Intuitively, prompting aims at reformulating downstream tasks to the pre-training task. Therefore, it would be more natural if the prompt tuning shares the same objective with the pre-training task. As suggested in GraphPrompt \cite{liu2023graphprompt}, the authors use a similar loss function to learn prompts. Similarly, GPPT \cite{sun2022gppt} and VNT \cite{tan2023virtual} adopt the same loss function (Cross-Entropy) as their link prediction and node classification tasks, respectively. HetGPT \cite{ma2023hetgpt} and PSP \cite{ge2024psp} use a node-level contrastive loss to learn their prompt tokens because their pre-training task is also conducted by the same contrastive task (node pair comparison). PGCL \cite{gong2023prompt} introduces graph-level loss to align with the pre-training task. ULTRA-DP \cite{chen2023ultradp} develop two pre-training tasks including edge prediction and neighboring prediction, each of which corresponds to one task embedding. In the pre-training phase, they randomly select a task and then integrate specific task-related embeddings into the prompt tokens. These learnable task embeddings are then trained with the graph model. Newer tuning strategies shift attention from prompt placement to prompt optimization. DAGPrompT \cite{chen2025dagprompt} combines graph LoRA with hop-wise similarity aggregation and learns from fused similarity scores across layers. RELIEF \cite{zhu2024relief} treats the entire prompt matrix as a feature prompt and tunes it with hierarchical PPO, using the downstream loss drop as the reward signal.



\subsection{Further Discussion}\label{subsec:pdiscussion}

\textit{\dotuline{What are their connections, pros and cons?}}

The preceding subsections describe prompt form, answering function, and tuning strategy separately. In practice, these choices are coupled: a prompt that is easy to insert may have limited expressive power, while a more structured prompt may require stronger assumptions about the downstream graph.

GPF \cite{fang2022prompt} shows the appeal of a minimal prompt: a single learnable vector can be added to node features and reused across different pre-training and downstream tasks. Its simplicity also exposes its main limitation. A prompt shared by all nodes has limited expressiveness and may not generalize well across diverse graph instances. Learning an independent prompt for each node would increase flexibility, but it would also be parameter-inefficient. GPF-Plus \cite{fang2023universal} addresses this tension by learning $K$ basis vectors and composing node-level prompts from them, which brings it closer in spirit to the prompt-token set used in All in One \cite{sun2023all}.


HetGPT \cite{ma2023hetgpt} extends prompt tokens to type-specific format, which can deal with graph prompting in heterogeneous data. However, they can only deal with node classification tasks. To this end, GraphPrompt \cite{liu2023graphprompt} reformulates link prediction to graph pair similarity task. It is worth noticing that the role of their prompt token is very similar to the project vector in the graph attention network. There are also some attention-based graph-pooling methods, which share the same motivation. The difference is that the authors claim the graph-pooling component in the pre-training stage might not fit other downstream tasks, thus needing additional prompts to redirect the graph-pooling component in the graph model.  

GPPT \cite{sun2022gppt} can be viewed as a specific instance of the broader All in One framework \cite{sun2023all}. If the prompt graph in All in One is reduced to isolated tokens associated with node classes and the resulting graphs are treated as complete graphs, its structure becomes close to the GPPT format. This makes it possible to reuse edge-level pretexts for node classification. The limitation is scope: GPPT is tied to binary edge prediction pretexts and mainly serves node classification. GraphPrompt and All in One cover a wider range of graph tasks, but GraphPrompt, GPF, and GPF-Plus often require an additional task-specific module during adaptation. All in One and GPPT instead rely more on task templates and input manipulation, which reduces their dependence on the form of the downstream classifier.


PRODIGY \cite{huang2023prodigy} connects these task-template methods to graph in-context learning. Its data graphs are close to the induced graphs used in All in One and GraphPrompt, while its pre-training task, predicting the link between data tokens and label tokens, follows the same general intuition as GPPT. The main advantage is efficiency: the prompt is fixed and does not require downstream tuning, which makes it suitable for transferring knowledge across datasets. The limitation is flexibility. When the downstream domain differs substantially from the pre-training domain, a non-tunable prompt may not provide enough adaptation capacity. ULTRA-DP \cite{chen2023ultradp} takes almost the opposite route by tuning prompts during both pre-training and downstream adaptation. Its task embeddings are first learned in pre-training and then used to initialize downstream prompts. In this case, the prompt does not simply reformulate the downstream task as a pretext task; it also helps select suitable pre-training tasks from a task pool. This contrast leaves a central trade-off: prompt methods need enough flexibility to adapt, but too much tuning can weaken their efficiency and generality.


VNT \cite{tan2023virtual} sits between explicit prompt insertion and model-internal prompt interaction. Instead of defining prompt-to-node links by hand, it concatenates prompt tokens with the original node set and lets the graph transformer compute their correlations through self-attention. This avoids a manually chosen connection threshold, but it also ties the method to transformer backbones. Since the prompt tokens have no explicit links to the original graph, it can also be harder to combine VNT with graph transformer variants that rely on positional or structural encodings.

This shift explains why recent methods no longer fit neatly into the original token/graph dichotomy. GPF and GPF-Plus are easy to apply because they only modify node features, but they treat graph structure mostly as something handled by the frozen encoder. Self-Pro \cite{gong2024selfpro}, PSP \cite{ge2024psp}, SUPT \cite{lee2024supt}, and EdgePrompt \cite{fu2025edge} move further toward structure-aware prompting: they make prompts depend on graph semantics, class prototypes, local neighborhoods, or edges. This gives the prompt more access to graph structure, but it also makes the design less plug-and-play. The method may now depend on neighborhood construction, prototype quality, message-passing layers, or reliable structural signals.

A similar transition appears in prompt optimization. Earlier methods mainly ask whether downstream tasks can be reformulated to match a pretext task. DAGPrompT \cite{chen2025dagprompt}, IGAP \cite{yan2024igap}, RELIEF \cite{zhu2024relief}, GraphPrompter \cite{lv2025graphprompter}, and UniPrompt \cite{huang2025oneprompt} instead focus on what happens when the downstream graph distribution differs from pre-training. DAGPrompT adapts prompts across hops and distributions, IGAP uses spectral alignment to reduce feature and structure gaps, RELIEF learns where and how strongly to prompt through reinforcement learning, GraphPrompter optimizes prompt selection for graph in-context learning, and UniPrompt studies universal adaptation across pre-trained models and domains. These methods are more flexible than early task-template methods, but their gains come with extra assumptions or cost: spectral decompositions may not scale easily, RL-based prompt search adds training complexity, graph in-context methods rely on prompt retrieval quality, and universal adaptation still depends on how broad the pre-training distribution is. These trade-offs motivate the specialized settings discussed next, where graph type, temporal dynamics, modality, and domain shift all affect what a useful prompt should look like.


\subsection{Extensions and Specialized Settings}\label{subsec:pextensions}

The methods above mainly target general graph prompting. Recent work also studies settings where the graph itself has additional constraints, such as heterogeneous node types, signed edges, or temporal dynamics.

\textbf{Heterogeneous Graph Prompting.} Heterogeneous graphs require prompts to account for node types, edge types, and relation-specific semantics. HetGPT \cite{ma2023hetgpt} adds type-specific feature tokens to prompt tokens. HGPROMPT \cite{yu_hgprompt_2024} bridges homogeneous and heterogeneous graph prompting by aligning structural patterns across relation types. HePa \cite{jia_hepa_2025} handles node-, edge-, and graph-level classification on heterogeneous graphs. HGMP \cite{jiao_hgmp_2025} treats heterogeneous graph prompting as a multi-task problem with contrastive pre-training. MultiHGPT \cite{wang_multihgpt_2025} uses task-specific prompts for multi-task heterogeneous graph learning. CLEAR \cite{wang2025clear} introduces cluster prompts and injects them into meta-paths, which brings high-order heterogeneous semantics into the prompt design.

\textbf{Signed and Temporal Graphs.} Signed graphs carry both positive and negative edges, which requires changing the prompting objective. SGPT \cite{zhai2024sgpt} adapts prompt tuning to signed graphs under few-shot settings. STGP \cite{hu_prompt-based_2024} uses prompts for spatio-temporal graph transfer, and Event-Aware Prompt Learning \cite{yu_event-aware_2025} applies prompts to event-driven dynamic graphs.

